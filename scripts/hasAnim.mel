global proc int hasAnim(string $obj)
{
	int $hasAnim = 0;
	int $numFrames
	if (`objExists $obj`)
	{
		$numFrames = `keyframe  -q -keyframeCount $obj`;
		if ($numFrames > 0){$hasAnim = 1;}
	}
	return $hasAnim;
}