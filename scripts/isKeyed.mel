global proc int isKeyed(string $obj, string $att, float $frame)
{
	int $isKeyed = 0;
	string $objatt = ($obj + "." + $att);

	float $k[] = `keyframe -hi "none" -t $frame -q -tc $objatt`;
	if (size($k)){$isKeyed = 1;}
	
	return $isKeyed;
}