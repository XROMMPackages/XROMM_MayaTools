global proc int isKeyed(string $obj, string $att, float $frame)
{
	int $isKeyed = 0;

	//float $frame = `currentTime -q`;
	string $parents[] = `listRelatives -ap $obj`;
	float $keysOnObject[] = `keyframe -q -at $att -tc $parents $obj`;
	
	for ($k in $keysOnObject)
	{
		if ($k == $frame){$isKeyed = 1;}
	}
	
	return $isKeyed;
}